import { WalletNotInstalledDeeplinkPipe } from './wallet-not-installed-deeplink.pipe';

describe('WalletNotInstalledDeeplinkPipe', () => {
  it('create an instance', () => {
    const pipe = new WalletNotInstalledDeeplinkPipe();
    expect(pipe).toBeTruthy();
  });
});
