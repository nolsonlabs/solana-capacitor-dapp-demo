import { WalletInstalledPipe } from './wallet-installed.pipe';

describe('WalletInstalledPipe', () => {
  it('create an instance', () => {
    const pipe = new WalletInstalledPipe();
    expect(pipe).toBeTruthy();
  });
});
